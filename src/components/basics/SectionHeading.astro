---
interface Props {
	title: string;
	subtitle: string;
}

const { title, subtitle, ...rest } = Astro.props;
---

<div class="text-center" {...rest}>
	<h1 class="font-bold text-[30px] text-deepblue-7 dark:text-gravel-1">{title}</h1>
	<p class="mt-2">{subtitle}</p>
</div>